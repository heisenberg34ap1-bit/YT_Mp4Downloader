# 要件定義書

## 1. 文書情報

- システム名: MP4 Downloader
- 対象: Android アプリ
- 目的: 指定した動画 URL から端末へ動画ファイルを保存できるようにする

## 2. 背景

ユーザーが動画 URL を入力し、保存先を選ぶだけで端末に動画を保存できる最小構成の Android アプリを提供する。  
初期対象は、直接取得できる MP4 ファイルと YouTube URL とする。

## 3. スコープ

本要件定義の対象:

- MP4 直リンクの保存
- YouTube URL からの動画ストリーム解決と保存
- 保存先選択 UI
- 入力検証、エラー表示、完了通知

本要件定義の対象外:

- バックグラウンド常駐ダウンロード
- 複数同時ダウンロード
- ダウンロード一時停止、再開
- ログインが必要な動画サイト対応
- YouTube 以外の動画サイト解析

## 4. 想定ユーザー

- 動画 URL を端末へ保存したい一般利用者
- Android Studio 上で本アプリを保守する開発者

## 5. 業務要件

- ユーザーは URL を 1 件入力してダウンロードを開始できること
- ユーザーは保存先と保存ファイル名を選択できること
- アプリは URL 種別に応じて処理を分岐できること
- アプリは失敗時に理由をユーザーへ通知できること

## 6. 機能要件

### 6.1 URL 入力

- テキスト入力欄を提供すること
- 未入力で開始した場合はエラー表示すること
- HTTP/HTTPS 以外の URL は拒否すること

### 6.2 URL 判定

- YouTube URL を判定できること
- YouTube 以外は `.mp4` 拡張子 URL のみ許可すること
- 条件に合わない URL はエラー表示すること

### 6.3 YouTube 動画解決

- `NewPipeExtractor` を利用して動画情報を取得すること
- 利用可能な動画ストリームからダウンロード対象を 1 件選定すること
- 選定優先順位は以下とすること
- 音声付きストリームを優先
- MP4 形式を優先
- ビットレートが高いものを優先
- ストリームが取得できない場合はエラー通知すること

### 6.4 保存先選択

- Android の Storage Access Framework を利用すること
- ユーザーが保存ダイアログをキャンセルした場合は処理を終了すること
- 保存先 URI が取得できた場合のみダウンロードを開始すること

### 6.5 ダウンロード

- `OkHttp` を利用してファイルを取得すること
- 取得データを選択された保存先へ書き込むこと
- YouTube 実体 URL が `googlevideo.com` の場合は `Referer` と `Origin` を付与すること
- SSL 関連例外が発生した場合は `Connection: close` を付けて再試行すること

### 6.6 ファイル名生成

- MP4 直リンクでは URL 末尾からファイル名を推定すること
- 不正文字を除去した安全なファイル名へ正規化すること
- ファイル名が空になる場合はタイムスタンプベースの代替名を付与すること
- YouTube 動画では動画タイトルをベースにファイル名を生成すること

### 6.7 通知とエラー表示

- ダウンロード開始時に通知を行うこと
- 完了時に通知を行うこと
- 失敗時は原因メッセージを表示すること

## 7. 非機能要件

### 7.1 対応環境

- Android 7.0 以上
- `minSdk = 24`
- `targetSdk = 34`

### 7.2 性能

- 単一動画のダウンロードを正常に完了できること
- UI スレッドでネットワーク I/O を実行しないこと

### 7.3 保守性

- URL 判定、YouTube 解決、ダウンロード処理を分離すること
- 主要ロジックに対して単体テストを用意できる構成であること

### 7.4 セキュリティ

- 必要最小限の権限のみを利用すること
- 保存先はユーザー選択方式とし、アプリが任意パスへ直接保存しないこと

## 8. 制約事項

- YouTube 抽出は外部ライブラリ仕様に依存する
- ネットワーク状態や動画公開状態により取得結果が変動する
- 直接 URL の MP4 判定はレスポンスヘッダーではなく URL 文字列ベースである
- 著作権、利用規約、法令順守は運用上の前提とする

## 9. 受け入れ条件

- 有効な MP4 URL を入力すると保存ダイアログが開くこと
- 有効な YouTube URL を入力すると保存可能な動画ストリームを解決できること
- ダウンロード完了後、保存先にファイルが存在すること
- 無効 URL、未入力、対象外 URL で適切なエラーが表示されること
- 保存ダイアログをキャンセルした場合、アプリがクラッシュしないこと

## 10. 既知の課題

- YouTube 依存のテストは外部要因で不安定になりうる
- ダウンロード進捗表示がない
- キャンセル機能がない
- 文字列リソースに文字化けが含まれている可能性があり、別途確認が必要
